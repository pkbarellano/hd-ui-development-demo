import {
    Typography,
} from "@mui/material";

const Logo = () => {

    return (
        <Typography variant="h5" component="div" sx={{ width: '100%' }}>
            Lamp Post
        </Typography>
    );
};

export default Logo;