import {
    Card,
    CardContent
} from '@mui/material';

import ComponentHeader from '../../../component/UI/ComponentHeader';

export const DashboardInterface = () => {

    return (
        <Card>
            <ComponentHeader />
            <CardContent>
            </CardContent>
        </Card>
    );
};