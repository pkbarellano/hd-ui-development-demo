import {
    Card,
    CardContent
} from '@mui/material';

import ComponentHeader from '../../component/UI/ComponentHeader';

export const Home = () => {

    return (
        <Card>
            <ComponentHeader />
            <CardContent>
            </CardContent>
        </Card>
    );
};