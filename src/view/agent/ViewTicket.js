import {
    Card,
    CardContent
} from '@mui/material';

import ComponentHeader from '../../component/UI/ComponentHeader';

export const ViewTicket = () => {

    return (
        <Card>
            <ComponentHeader />
            <CardContent>
            </CardContent>
        </Card>
    );
};