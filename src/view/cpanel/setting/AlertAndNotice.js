import {
    Card,
    CardContent
} from '@mui/material';

import ComponentHeader from '../../../component/UI/ComponentHeader';

export const AlertAndNotice = () => {

    return (
        <Card>
            <ComponentHeader />
            <CardContent>
            </CardContent>
        </Card>
    );
};