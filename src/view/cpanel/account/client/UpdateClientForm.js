import ModalUI from '../../../../component/UI/Modal';

const UpdateClientForm = ({ selectedRows, openUpdateModalState, setOpenUpdateModalState }) => {

    return (
        <>
        </>
    );
};

export default UpdateClientForm;